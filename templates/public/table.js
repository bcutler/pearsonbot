// partition by number of rows in row_totals

window.onload = function() {
	var r = Raphael("holder", 620,800);
	$(".compare").click(function(){
		if ($(this).html == $(".compare_selected").html)
		
		// check that a .with elem has been clicked as well.
		// if not, just highlight the .compare elem.
		// if so, check to see if the .compare elem is the same one as before.
		// if not, make an ajax request for the table data.
	});
	$(".with").click(function() {
		// check that a .compare elem has been clicked as well.
		// if not, just highlight the .with elem.
		// if so, ceck to see if the .with elem is the same one as before.
		// if not, make an ajax request for the table data.
		
	})
}